---
name: Branch Push CI

on:
  # push:
  #   # Run on any branch except the principal 'main' branch
  #   # branches-ignore:
  #   #   - main
  workflow_dispatch:    

jobs:
  build:
    uses: ./.github/workflows/ci-workflow.yml
    with:
      run_web: true
      run_firmware: true
