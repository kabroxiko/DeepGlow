---
name: Pull Request CI

on:
  pull_request_target:
    types: [opened, synchronize, reopened, edited]
    branches:
      - main

jobs:
  build:
    if: ${{ github.actor == 'renovate[bot]' && github.event.pull_request.state != 'closed' }}
    uses: ./.github/workflows/ci-workflow.yml
    with:
      run_web: true
      run_firmware: true
